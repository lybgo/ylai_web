---
/**
 * Section 27 In-depth Configuration of Condition Logic for "Auto Task"
 * slug: 27-condition-logic
 */
---

<section>
    <div class="description">
        <p>
            Master the configuration of complex condition judgments,
            multi-condition combinations, and advanced logic operations.
        </p>
    </div>
    <div class="learning-points">
        <h2>📚 Learning Points</h2>
        <ul>
            <li>
                Composite Condition Configuration: Master the combination of AND
                and OR logic relationships to achieve "Trigger when both A and B
                are met"
            </li>
            <li>
                Time Period Condition: Configure tasks to trigger "during a
                certain time period" or at "sunrise/sunset time"
            </li>
            <li>
                Loop Condition: Configure cycle tasks that "repeat at certain
                time intervals"
            </li>
            <li>
                Status Maintenance Condition: Configure the task to trigger only
                "after a certain condition is continuously met for a period of
                time"
            </li>
        </ul>
    </div>
</section>
<style>
    .learning-points {
        margin-bottom: 2rem;
    }

    .learning-points h2 {
        font-size: 1.25rem;
        font-weight: 600;
        color: var(--text-dark);
        margin: 0 0 1rem;
    }

    .learning-points ul {
        list-style: none;
        padding: 0;
        margin: 0;
    }

    .learning-points li {
        position: relative;
        padding: 0.875rem 0.875rem 0.875rem 2rem;
        color: var(--text-muted);
        font-size: 0.9rem;
        line-height: 1.6;
        border-bottom: 1px solid var(--border-light);
    }

    .learning-points li:last-child {
        border-bottom: none;
    }

    .learning-points li::before {
        content: "✓";
        position: absolute;
        left: 0;
        color: var(--primary);
        font-weight: 600;
    }

    .description {
        font-size: 1.1rem;
        color: var(--text-dark);
        line-height: 1.7;
        margin-bottom: 2rem;
        padding: 1.5rem;
        background: linear-gradient(135deg, #f0f7ff 0%, #e6f0fa 100%);
        border-radius: 12px;
        border-left: 4px solid var(--primary);
    }

    .description p {
        margin: 0;
    }
</style>
