---
/**
 * Section 11 "One-Click Execution" Quick Start
 * slug: 11-one-click
 */
---

<section>
    <div class="description">
        <p>
            Master the creation, editing, and calling of "One-Click Execution"
            tasks to achieve multi-device collaborative control.
        </p>
    </div>
    <div class="learning-points">
        <h2>📚 Learning Points</h2>
        <ul>
            <li>
                "One-Click Execution" Concept: Explain it as a composite
                operation set for manual triggers or automated task calls
            </li>
            <li>
                Create "One-Click Execution" Tasks: Configure linked operations
                of multi-channel outputs (relays, PWM)
            </li>
            <li>
                Virtual Switch Binding: Bind "One-Click Execution" tasks to
                virtual switches on the app interface for quick operations
            </li>
            <li>
                Counter Concept: What a counter is and the role of the power-off
                save (power-down hold) function
            </li>
            <li>
                Use and Reference of Counters: Introduce addition (+) and
                subtraction (-) operations of counters, and how to reference
                counter values in "One-Click Execution" tasks
            </li>
        </ul>
    </div>
</section>
<style>
    .learning-points {
        margin-bottom: 2rem;
    }

    .learning-points h2 {
        font-size: 1.25rem;
        font-weight: 600;
        color: var(--text-dark);
        margin: 0 0 1rem;
    }

    .learning-points ul {
        list-style: none;
        padding: 0;
        margin: 0;
    }

    .learning-points li {
        position: relative;
        padding: 0.875rem 0.875rem 0.875rem 2rem;
        color: var(--text-muted);
        font-size: 0.9rem;
        line-height: 1.6;
        border-bottom: 1px solid var(--border-light);
    }

    .learning-points li:last-child {
        border-bottom: none;
    }

    .learning-points li::before {
        content: "✓";
        position: absolute;
        left: 0;
        color: var(--primary);
        font-weight: 600;
    }

    .description {
        font-size: 1.1rem;
        color: var(--text-dark);
        line-height: 1.7;
        margin-bottom: 2rem;
        padding: 1.5rem;
        background: linear-gradient(135deg, #f0f7ff 0%, #e6f0fa 100%);
        border-radius: 12px;
        border-left: 4px solid var(--primary);
    }

    .description p {
        margin: 0;
    }
</style>
