---
/**
 * Section 16 "Auto Task" Automation Control Quick Start
 * slug: 16-auto-task-intro
 */
---

<section>
    <div class="description">
        <p>
            Master the quick creation of automation tasks (If-Then) based on
            sensor inputs and logical judgments.
        </p>
    </div>
    <div class="learning-points">
        <h2>📚 Learning Points</h2>
        <ul>
            <li>
                "Auto Task" Concept: Explain it as condition-driven automation
                control logic
            </li>
            <li>
                Water Level Condition Example: Configure a simple logic "When
                the water level sensor input is low, turn on the pump relay"
            </li>
            <li>
                Temperature Condition Example: Configure temperature control
                logic "When the temperature is higher than the threshold, turn
                on the exhaust fan"
            </li>
            <li>
                Counter Condition Example: Configure logic "When the counter
                reaches the set value (e.g., cumulative pulse number), execute
                an action"
            </li>
            <li>Task Enablement and Disablement Management</li>
        </ul>
    </div>
</section>
<style>
    .learning-points {
        margin-bottom: 2rem;
    }

    .learning-points h2 {
        font-size: 1.25rem;
        font-weight: 600;
        color: var(--text-dark);
        margin: 0 0 1rem;
    }

    .learning-points ul {
        list-style: none;
        padding: 0;
        margin: 0;
    }

    .learning-points li {
        position: relative;
        padding: 0.875rem 0.875rem 0.875rem 2rem;
        color: var(--text-muted);
        font-size: 0.9rem;
        line-height: 1.6;
        border-bottom: 1px solid var(--border-light);
    }

    .learning-points li:last-child {
        border-bottom: none;
    }

    .learning-points li::before {
        content: "✓";
        position: absolute;
        left: 0;
        color: var(--primary);
        font-weight: 600;
    }

    .description {
        font-size: 1.1rem;
        color: var(--text-dark);
        line-height: 1.7;
        margin-bottom: 2rem;
        padding: 1.5rem;
        background: linear-gradient(135deg, #f0f7ff 0%, #e6f0fa 100%);
        border-radius: 12px;
        border-left: 4px solid var(--primary);
    }

    .description p {
        margin: 0;
    }
</style>
