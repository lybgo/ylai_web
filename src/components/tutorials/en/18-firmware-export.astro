---
/**
 * Section 18 Firmware Export/Import and Backup
 * slug: 18-firmware-export
 */
---

<section>
    <div class="description">
        <p>
            Master the backup and migration methods of device configurations to
            ensure logic safety and facilitate multi-device deployment.
        </p>
    </div>
    <div class="learning-points">
        <h2>📚 Learning Points</h2>
        <ul>
            <li>
                Importance of Configuration Backup: Prevent data loss caused by
                accidental operation or device replacement
            </li>
            <li>
                Exporting Configurations: How to export the current automated
                tasks, counter settings, etc., within the app
            </li>
            <li>
                Importing and Restoring: Demonstration of the process of
                importing backup configuration files into new devices or
                restored devices
            </li>
            <li>
                Multi-device Batch Deployment: How to use the import/export
                function to quickly deploy the same control logic to multiple
                IOTAutoMate devices
            </li>
        </ul>
    </div>
</section>
<style>
    .learning-points {
        margin-bottom: 2rem;
    }

    .learning-points h2 {
        font-size: 1.25rem;
        font-weight: 600;
        color: var(--text-dark);
        margin: 0 0 1rem;
    }

    .learning-points ul {
        list-style: none;
        padding: 0;
        margin: 0;
    }

    .learning-points li {
        position: relative;
        padding: 0.875rem 0.875rem 0.875rem 2rem;
        color: var(--text-muted);
        font-size: 0.9rem;
        line-height: 1.6;
        border-bottom: 1px solid var(--border-light);
    }

    .learning-points li:last-child {
        border-bottom: none;
    }

    .learning-points li::before {
        content: "✓";
        position: absolute;
        left: 0;
        color: var(--primary);
        font-weight: 600;
    }

    .description {
        font-size: 1.1rem;
        color: var(--text-dark);
        line-height: 1.7;
        margin-bottom: 2rem;
        padding: 1.5rem;
        background: linear-gradient(135deg, #f0f7ff 0%, #e6f0fa 100%);
        border-radius: 12px;
        border-left: 4px solid var(--primary);
    }

    .description p {
        margin: 0;
    }
</style>
