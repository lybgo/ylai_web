---
/**
 * Section 6 IOTAutoMate Pro Hardware Model Selection Guide
 * slug: 06-hardware-selection
 */
---

<section>
    <div class="description">
        <p>
            Understand the hardware model selection guide of IOTAutoMate Pro to
            prepare for correctly choosing the hardware model of IOTAutoMate Pro
            for future projects.
        </p>
    </div>
    <div class="learning-points">
        <h2>📚 Learning Points</h2>
        <ul>
            <li>
                Input/Output I/O Point Model: Choose the appropriate number of
                relay channels, PWM channels, switching input channels, etc.,
                based on project requirements (e.g., 6+4 channels)
            </li>
            <li>
                Network Communication Methods: Selection and applicable
                scenarios for WiFi version, 4G, BLE version, etc.
            </li>
            <li>
                Power Supply Voltage Selection: Choices between standard USB
                5V/DC 5V power supply, wide voltage version (DC 7V~36V), or AC
                220V version
            </li>
            <li>
                Optional Functional Modules: Considerations for selecting RS485
                bus functionality and 433MHz wireless radio frequency modules
            </li>
            <li>
                Output Type Configuration: Optional types for PWM output ports
                (e.g., 0-10V/0-20mA analog output)
            </li>
        </ul>
    </div>
</section>
<style>
    .learning-points {
        margin-bottom: 2rem;
    }

    .learning-points h2 {
        font-size: 1.25rem;
        font-weight: 600;
        color: var(--text-dark);
        margin: 0 0 1rem;
    }

    .learning-points ul {
        list-style: none;
        padding: 0;
        margin: 0;
    }

    .learning-points li {
        position: relative;
        padding: 0.875rem 0.875rem 0.875rem 2rem;
        color: var(--text-muted);
        font-size: 0.9rem;
        line-height: 1.6;
        border-bottom: 1px solid var(--border-light);
    }

    .learning-points li:last-child {
        border-bottom: none;
    }

    .learning-points li::before {
        content: "✓";
        position: absolute;
        left: 0;
        color: var(--primary);
        font-weight: 600;
    }

    .description {
        font-size: 1.1rem;
        color: var(--text-dark);
        line-height: 1.7;
        margin-bottom: 2rem;
        padding: 1.5rem;
        background: linear-gradient(135deg, #f0f7ff 0%, #e6f0fa 100%);
        border-radius: 12px;
        border-left: 4px solid var(--primary);
    }

    .description p {
        margin: 0;
    }
</style>
