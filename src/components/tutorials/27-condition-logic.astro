---
/**
 * 第 27 节 "自动任务"的条件逻辑配置深入
 * slug: 27-condition-logic
 */
---

<section>
    <div class="description">
        <p>掌握复杂的条件判断、多条件组合和高级逻辑运算的配置。</p>
    </div>
    <div class="learning-points">
        <h2>📚 学习要点</h2>
        <ul>
            <li>
                复合条件配置:掌握AND、OR逻辑关系的组合使用,实现"满足A且满足B时触发"
            </li>
            <li>
                时间段条件:配置"在某一时间段内"或"在日出/日落时间"触发任务
            </li>
            <li>
                循环条件:配置"在一定时间间隔内重复执行"的循环任务
            </li>
            <li>
                状态保持条件:配置"某条件持续满足一段时间后"才触发任务
            </li>
        </ul>
    </div>
</section>
<style>
    .learning-points {
        margin-bottom: 2rem;
    }

    .learning-points h2 {
        font-size: 1.25rem;
        font-weight: 600;
        color: var(--text-dark);
        margin: 0 0 1rem;
    }

    .learning-points ul {
        list-style: none;
        padding: 0;
        margin: 0;
    }

    .learning-points li {
        position: relative;
        padding: 0.875rem 0.875rem 0.875rem 2rem;
        color: var(--text-muted);
        font-size: 0.9rem;
        line-height: 1.6;
        border-bottom: 1px solid var(--border-light);
    }

    .learning-points li:last-child {
        border-bottom: none;
    }

    .learning-points li::before {
        content: "✓";
        position: absolute;
        left: 0;
        color: var(--primary);
        font-weight: 600;
    }

    .description {
        font-size: 1.1rem;
        color: var(--text-dark);
        line-height: 1.7;
        margin-bottom: 2rem;
        padding: 1.5rem;
        background: linear-gradient(135deg, #f0f7ff 0%, #e6f0fa 100%);
        border-radius: 12px;
        border-left: 4px solid var(--primary);
    }

    .description p {
        margin: 0;
    }
</style>
