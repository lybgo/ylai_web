---
import Layout from "../../layouts/Layout.astro";

const lang = "zh";
---

<Layout
    title="宜启智控Pro - 智能物联网自动化控制器 | 无需编程"
    description="无需编程的智能物联网自动化万能控制器，手机APP轻松配置，支持95%市场传感器，WiFi/4G/蓝牙多种联网方式"
    lang={lang}
>
    <!-- Hero Section -->
    <section class="hero">
        <div class="hero-container">
            <div class="hero-content">
                <h1 class="hero-title animate-fade-in-up">
                    让控制变得简单<br />
                    <span class="hero-title-accent">让万物互联触手可及</span>
                </h1>
                <p
                    class="hero-subtitle animate-fade-in-up"
                    style="animation-delay: 0.1s"
                >
                    无需编程，手机APP轻松配置——宜启智控Pro，您的智能物联网自动化万能控制器
                </p>
                <div
                    class="hero-cta animate-fade-in-up"
                    style="animation-delay: 0.2s"
                >
                    <a href="/zh/buy" class="btn btn-primary">立即购买</a>
                    <a href="/zh/tutorials" class="btn btn-secondary"
                        >快速入门</a
                    >
                    <a href="/zh/mqtt" class="btn btn-ghost">查看文档</a>
                </div>
            </div>
            <div
                class="hero-image animate-fade-in-up"
                style="animation-delay: 0.3s"
            >
                <img
                    src="/images/products/6+4路45度ok1.png"
                    alt="宜启智控Pro 6+4路控制器"
                />
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section class="features">
        <div class="features-container">
            <h2 class="section-title">为什么选择宜启智控</h2>
            <div class="features-grid">
                <div class="feature-card">
                    <div class="feature-icon">📱</div>
                    <h3 class="feature-title">告别编程烦恼</h3>
                    <p class="feature-desc">
                        无需写代码，手机APP点击配置，小白也能秒变控制大师
                    </p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">🔌</div>
                    <h3 class="feature-title">兼容万物传感</h3>
                    <p class="feature-desc">
                        支持市面95%传感器，4-20mA/0-10V/RS485/IIC通吃
                    </p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">🌐</div>
                    <h3 class="feature-title">随时随地掌控</h3>
                    <p class="feature-desc">
                        WiFi/4G/蓝牙多种联网，涂鸦生态+自有APP，远程控制稳如老狗
                    </p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">⚡</div>
                    <h3 class="feature-title">即插即用秒部署</h3>
                    <p class="feature-desc">
                        比传统PLC简单10倍，1天速成专业控制系统
                    </p>
                </div>
            </div>
        </div>
    </section>

    <!-- Product Showcase -->
    <section class="showcase">
        <div class="showcase-container">
            <h2 class="section-title">产品展示</h2>
            <div class="product-gallery" x-data="{ activeIndex: 0 }">
                <div class="gallery-main">
                    <img
                        x-show="activeIndex === 0"
                        src="/images/products/6+4路45度ok1.png"
                        alt="6+4路型号"
                    />
                    <img
                        x-show="activeIndex === 1"
                        src="/images/products/4路45度.png"
                        alt="4路型号"
                    />
                    <img
                        x-show="activeIndex === 2"
                        src="/images/products/3+2路45度.png"
                        alt="3+2路型号"
                    />
                    <img
                        x-show="activeIndex === 3"
                        src="/images/products/2路45度.png"
                        alt="2路型号"
                    />
                </div>
                <div class="gallery-thumbs">
                    <button
                        @click="activeIndex = 0"
                        :class="{ active: activeIndex === 0 }"
                    >
                        <img src="/images/products/6+4路正面.png" alt="6+4路" />
                        <span>6+4路</span>
                    </button>
                    <button
                        @click="activeIndex = 1"
                        :class="{ active: activeIndex === 1 }"
                    >
                        <img src="/images/products/4路正面.png" alt="4路" />
                        <span>4路</span>
                    </button>
                    <button
                        @click="activeIndex = 2"
                        :class="{ active: activeIndex === 2 }"
                    >
                        <img src="/images/products/3+2路正面.png" alt="3+2路" />
                        <span>3+2路</span>
                    </button>
                    <button
                        @click="activeIndex = 3"
                        :class="{ active: activeIndex === 3 }"
                    >
                        <img src="/images/products/2路正面.png" alt="2路" />
                        <span>2路</span>
                    </button>
                </div>
            </div>
            <div class="product-specs">
                <span class="spec-badge">6路继电器</span>
                <span class="spec-badge">4路PWM</span>
                <span class="spec-badge">6路输入</span>
                <span class="spec-badge">RS485</span>
                <span class="spec-badge">433MHz</span>
                <span class="spec-badge">IIC</span>
            </div>
        </div>
    </section>

    <!-- Use Cases -->
    <section class="use-cases">
        <div class="use-cases-container">
            <h2 class="section-title">应用场景</h2>
            <div class="cases-grid">
                <a href="/zh/cases#agriculture" class="case-card">
                    <div class="case-icon">🌾</div>
                    <h3 class="case-title">智慧农业</h3>
                    <p class="case-subtitle">让庄稼自己喝水</p>
                    <p class="case-desc">自动灌溉、温室环控、水产养殖</p>
                </a>
                <a href="/zh/cases#building" class="case-card">
                    <div class="case-icon">🏢</div>
                    <h3 class="case-title">智慧楼宇</h3>
                    <p class="case-subtitle">让建筑自己思考</p>
                    <p class="case-desc">人来亮灯、节能管理、安防联动</p>
                </a>
                <a href="/zh/cases#industrial" class="case-card">
                    <div class="case-icon">🏭</div>
                    <h3 class="case-title">工业控制</h3>
                    <p class="case-subtitle">让设备自己说话</p>
                    <p class="case-desc">变频器控制、数据采集、设备改造</p>
                </a>
                <a href="/zh/cases#education" class="case-card">
                    <div class="case-icon">📚</div>
                    <h3 class="case-title">教学实训</h3>
                    <p class="case-subtitle">让学习不再枯燥</p>
                    <p class="case-desc">物联网教学套件、实训平台</p>
                </a>
            </div>
        </div>
    </section>

    <!-- Quick Start -->
    <section class="quick-start">
        <div class="quick-start-container">
            <h2 class="section-title">三步快速上手</h2>
            <div class="steps-grid">
                <div class="step-card">
                    <div class="step-number">1</div>
                    <h3 class="step-title">连接</h3>
                    <p class="step-desc">USB供电，蓝牙连接APP</p>
                </div>
                <div class="step-arrow">→</div>
                <div class="step-card">
                    <div class="step-number">2</div>
                    <h3 class="step-title">配置</h3>
                    <p class="step-desc">拖拽式配置自动化逻辑</p>
                </div>
                <div class="step-arrow">→</div>
                <div class="step-card">
                    <div class="step-number">3</div>
                    <h3 class="step-title">控制</h3>
                    <p class="step-desc">随时随地远程监控</p>
                </div>
            </div>
            <a href="/zh/tutorials" class="btn btn-primary btn-lg"
                >0基础1天入门 →</a
            >
        </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section">
        <div class="cta-container">
            <h2 class="cta-title">还在为复杂的控制逻辑发愁？</h2>
            <p class="cta-subtitle">
                宜启智控Pro，让自动化控制像发朋友圈一样简单
            </p>
            <div class="cta-buttons">
                <a href="/zh/buy" class="btn btn-accent btn-lg">立即购买</a>
                <a href="/zh/compare" class="btn btn-outline btn-lg">对比选型</a
                >
            </div>
        </div>
    </section>
</Layout>

<style>
    /* Hero Section */
    .hero {
        background: linear-gradient(135deg, #f0f7ff 0%, #e6f0fa 100%);
        padding: 4rem 1.5rem;
        overflow: hidden;
    }

    .hero-container {
        max-width: 1200px;
        margin: 0 auto;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 3rem;
        align-items: center;
    }

    @media (max-width: 768px) {
        .hero-container {
            grid-template-columns: 1fr;
            text-align: center;
        }
    }

    .hero-title {
        font-size: 2.75rem;
        font-weight: 700;
        line-height: 1.2;
        color: var(--text-dark);
        margin: 0 0 1.5rem;
    }

    .hero-title-accent {
        color: var(--primary);
    }

    .hero-subtitle {
        font-size: 1.25rem;
        color: var(--text-muted);
        margin: 0 0 2rem;
        line-height: 1.6;
    }

    .hero-cta {
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
    }

    @media (max-width: 768px) {
        .hero-cta {
            justify-content: center;
        }
    }

    .hero-image img {
        max-width: 100%;
        height: auto;
        filter: drop-shadow(0 20px 40px rgba(0, 102, 204, 0.15));
    }

    /* Buttons */
    .btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 0.75rem 1.5rem;
        font-size: 1rem;
        font-weight: 600;
        border-radius: 10px;
        text-decoration: none;
        transition: all 0.2s ease;
        cursor: pointer;
        border: none;
    }

    .btn-lg {
        padding: 1rem 2rem;
        font-size: 1.125rem;
    }

    .btn-primary {
        background: linear-gradient(
            135deg,
            var(--primary) 0%,
            var(--primary-dark) 100%
        );
        color: white;
        box-shadow: 0 4px 14px rgba(0, 102, 204, 0.3);
    }

    .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(0, 102, 204, 0.4);
        color: white;
    }

    .btn-secondary {
        background: var(--primary-light);
        color: var(--primary);
    }

    .btn-secondary:hover {
        background: rgba(0, 102, 204, 0.2);
    }

    .btn-ghost {
        background: transparent;
        color: var(--text-muted);
        border: 1px solid var(--border-light);
    }

    .btn-ghost:hover {
        border-color: var(--primary);
        color: var(--primary);
    }

    .btn-accent {
        background: linear-gradient(135deg, var(--accent) 0%, #e85a2c 100%);
        color: white;
        box-shadow: 0 4px 14px rgba(255, 107, 53, 0.3);
    }

    .btn-accent:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(255, 107, 53, 0.4);
        color: white;
    }

    .btn-outline {
        background: transparent;
        color: var(--primary);
        border: 2px solid var(--primary);
    }

    .btn-outline:hover {
        background: var(--primary);
        color: white;
    }

    /* Section Title */
    .section-title {
        font-size: 2rem;
        font-weight: 700;
        text-align: center;
        margin: 0 0 3rem;
        color: var(--text-dark);
    }

    /* Features Section */
    .features {
        padding: 5rem 1.5rem;
        background: white;
    }

    .features-container {
        max-width: 1200px;
        margin: 0 auto;
    }

    .features-grid {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 2rem;
    }

    @media (max-width: 1024px) {
        .features-grid {
            grid-template-columns: repeat(2, 1fr);
        }
    }

    @media (max-width: 640px) {
        .features-grid {
            grid-template-columns: 1fr;
        }
    }

    .feature-card {
        text-align: center;
        padding: 2rem;
        background: var(--bg-light);
        border-radius: 16px;
        transition:
            transform 0.3s ease,
            box-shadow 0.3s ease;
    }

    .feature-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 30px rgba(0, 102, 204, 0.1);
    }

    .feature-icon {
        font-size: 3rem;
        margin-bottom: 1rem;
    }

    .feature-title {
        font-size: 1.25rem;
        font-weight: 600;
        color: var(--text-dark);
        margin: 0 0 0.75rem;
    }

    .feature-desc {
        font-size: 0.95rem;
        color: var(--text-muted);
        margin: 0;
        line-height: 1.6;
    }

    /* Showcase Section */
    .showcase {
        padding: 5rem 1.5rem;
        background: linear-gradient(180deg, #f8fafc 0%, #ffffff 100%);
    }

    .showcase-container {
        max-width: 1000px;
        margin: 0 auto;
        text-align: center;
    }

    .product-gallery {
        margin-bottom: 2rem;
    }

    .gallery-main {
        background: white;
        border-radius: 20px;
        padding: 2rem;
        margin-bottom: 1.5rem;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
    }

    .gallery-main img {
        max-height: 400px;
        width: auto;
        margin: 0 auto;
    }

    .gallery-thumbs {
        display: flex;
        justify-content: center;
        gap: 1rem;
        flex-wrap: wrap;
    }

    .gallery-thumbs button {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 0.5rem;
        padding: 0.75rem;
        background: white;
        border: 2px solid transparent;
        border-radius: 12px;
        cursor: pointer;
        transition: all 0.2s ease;
    }

    .gallery-thumbs button:hover,
    .gallery-thumbs button.active {
        border-color: var(--primary);
        box-shadow: 0 4px 12px rgba(0, 102, 204, 0.15);
    }

    .gallery-thumbs button img {
        width: 60px;
        height: 60px;
        object-fit: contain;
    }

    .gallery-thumbs button span {
        font-size: 0.875rem;
        font-weight: 500;
        color: var(--text-muted);
    }

    .product-specs {
        display: flex;
        justify-content: center;
        gap: 0.75rem;
        flex-wrap: wrap;
    }

    .spec-badge {
        padding: 0.5rem 1rem;
        background: var(--primary-light);
        color: var(--primary);
        font-size: 0.875rem;
        font-weight: 500;
        border-radius: 20px;
    }

    /* Use Cases Section */
    .use-cases {
        padding: 5rem 1.5rem;
        background: white;
    }

    .use-cases-container {
        max-width: 1200px;
        margin: 0 auto;
    }

    .cases-grid {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 2rem;
    }

    @media (max-width: 1024px) {
        .cases-grid {
            grid-template-columns: repeat(2, 1fr);
        }
    }

    @media (max-width: 640px) {
        .cases-grid {
            grid-template-columns: 1fr;
        }
    }

    .case-card {
        padding: 2rem;
        background: var(--bg-light);
        border-radius: 16px;
        text-decoration: none;
        transition: all 0.3s ease;
        border: 1px solid transparent;
    }

    .case-card:hover {
        transform: translateY(-5px);
        border-color: var(--primary);
        box-shadow: 0 10px 30px rgba(0, 102, 204, 0.1);
    }

    .case-icon {
        font-size: 2.5rem;
        margin-bottom: 1rem;
    }

    .case-title {
        font-size: 1.25rem;
        font-weight: 600;
        color: var(--text-dark);
        margin: 0 0 0.5rem;
    }

    .case-subtitle {
        font-size: 1rem;
        color: var(--primary);
        font-weight: 500;
        margin: 0 0 0.5rem;
    }

    .case-desc {
        font-size: 0.9rem;
        color: var(--text-muted);
        margin: 0;
    }

    /* Quick Start Section */
    .quick-start {
        padding: 5rem 1.5rem;
        background: linear-gradient(135deg, #f0f7ff 0%, #e6f0fa 100%);
        text-align: center;
    }

    .quick-start-container {
        max-width: 900px;
        margin: 0 auto;
    }

    .steps-grid {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 1.5rem;
        margin-bottom: 3rem;
        flex-wrap: wrap;
    }

    .step-card {
        flex: 1;
        min-width: 200px;
        max-width: 250px;
        padding: 2rem;
        background: white;
        border-radius: 16px;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
    }

    .step-number {
        width: 48px;
        height: 48px;
        background: var(--primary);
        color: white;
        font-size: 1.5rem;
        font-weight: 700;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 1rem;
    }

    .step-title {
        font-size: 1.25rem;
        font-weight: 600;
        color: var(--text-dark);
        margin: 0 0 0.5rem;
    }

    .step-desc {
        font-size: 0.95rem;
        color: var(--text-muted);
        margin: 0;
    }

    .step-arrow {
        font-size: 2rem;
        color: var(--primary);
        font-weight: 300;
    }

    @media (max-width: 768px) {
        .step-arrow {
            transform: rotate(90deg);
        }
    }

    /* CTA Section */
    .cta-section {
        padding: 3rem 1.5rem;
        background: var(--bg-light);
        text-align: center;
        border-top: 1px solid var(--border-light);
    }

    .cta-container {
        max-width: 700px;
        margin: 0 auto;
    }

    .cta-title {
        font-size: 2rem;
        font-weight: 700;
        color: var(--text-dark);
        margin: 0 0 1rem;
    }

    .cta-subtitle {
        font-size: 1.25rem;
        color: var(--text-muted);
        margin: 0 0 2rem;
    }

    .cta-buttons {
        display: flex;
        justify-content: center;
        gap: 1rem;
        flex-wrap: wrap;
    }

    .cta-section .btn-outline {
        color: var(--primary);
        border-color: var(--primary);
    }

    .cta-section .btn-outline:hover {
        background: var(--primary);
        color: white;
    }
</style>
