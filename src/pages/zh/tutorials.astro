---
import Layout from "../../layouts/Layout.astro";
import { tutorialSections, chapters } from "../../data/tutorialData";

const lang = "zh";

// 按章节分组
const groupedSections = chapters.map((chapter) => ({
    ...chapter,
    sections: tutorialSections.filter((s) => s.chapterNum === chapter.num),
}));
---

<Layout
    title="产品使用教程 - 宜启智控Pro"
    description="0基础1天入门：无需编程的自动化控制与物联网应用教程，宜启智控Pro控制器使用教程"
    lang={lang}
>

    <div class="content-wrapper">
        <nav class="sidebar">
            <h3 class="sidebar-title">目录</h3>
            <div class="sidebar-nav">
                {
                    groupedSections.map((chapter) => (
                        <div class="chapter-group">
                            <div class="chapter-title">{chapter.title}</div>
                            <ul class="section-list">
                                {chapter.sections.map((s) => (
                                    <li>
                                        <a href={`/zh/tutorials/${s.slug}`} class="section-link" >
                                            <span class="section-num"> {s.num} </span>
                                            <span class="section-name"> {s.title} </span>
                                        </a>
                                    </li>
                                ))}
                            </ul>
                        </div>
                    ))
                }
            </div>
        </nav>

        <main class="main-content">
            <!-- Course Intro -->
            <section class="intro-section">
                <div class="page-header">
                        <h1 class="page-title">0基础1天入门：<br />自动化控制与物联网应用教程（无需编程）<br />
                            & 宜启智控Pro控制器使用教程</h1>
                </div>
                <div class="intro-card">
                    <h2>🎯 课程目标</h2>
                    <p>
                        通过宜启智控Pro演示套件，使学生或工程人员系统掌握无需编程的自动化控制逻辑配置、
                        传感器数据采集、设备云端通信及多平台对接技术，具备快速搭建智慧项目或传统设备智能化升级的能力。
                    </p>
                </div>
                <div class="stats-grid">
                    <div class="stat-item">
                        <span class="stat-value">43</span>
                        <span class="stat-label">节课程</span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-value">4</span>
                        <span class="stat-label">大章节</span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-value">~13h</span>
                        <span class="stat-label">视频时长</span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-value">1天</span>
                        <span class="stat-label">速成入门</span>
                    </div>
                </div>
            </section>

            <!-- CTA -->
            <section class="cta-section">
                <h2>准备好开始学习了吗？</h2>
                <p>获取宜启智控Pro演示套件，跟随教程实操练习</p>
                <div class="cta-buttons">
                    <a
                        href={`/zh/tutorials/${tutorialSections[0].slug}`}
                        class="btn btn-primary btn-lg"
                    >
                        开始学习第一课 →
                    </a>
                    <a href="/zh/buy" class="btn btn-accent btn-lg"
                        >购买演示套件</a
                    >
                </div>
            </section>
        </main>
    </div>
</Layout>

<style>
    .page-header {
        padding: 3rem 1.5rem;
    }

    .container {
        max-width: 1200px;
        margin: 0 auto;
    }

    .page-title {
        color: #0066cc;
        font-size: 2rem;
        font-weight: 700;
        margin: 0 0 0.5rem;
    }

    .page-subtitle {
        font-size: 1rem;
        opacity: 0.9;
        margin: 0;
    }

    .content-wrapper {
        max-width: 1400px;
        margin: 0 auto;
        display: grid;
        grid-template-columns: 300px 1fr;
        gap: 2rem;
        padding: 2rem 1.5rem;
    }

    @media (max-width: 1024px) {
        .content-wrapper {
            grid-template-columns: 1fr;
        }
        .sidebar {
            position: static !important;
            max-height: none;
            border-right: none;
            border-bottom: 1px solid var(--border-light);
            padding-bottom: 1.5rem;
            margin-bottom: 1rem;
        }
    }

    /* Sidebar */
    .sidebar {
        position: sticky;
        top: 80px;
        height: fit-content;
        max-height: calc(100vh - 100px);
        overflow-y: auto;
        padding-right: 1rem;
        border-right: 1px solid var(--border-light);
    }

    .sidebar-title {
        font-size: 0.875rem;
        font-weight: 600;
        color: var(--text-muted);
        text-transform: uppercase;
        letter-spacing: 0.05em;
        margin: 0 0 1rem;
        padding-left: 0.5rem;
    }

    .sidebar-nav {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    .chapter-group {
        margin-bottom: 0.5rem;
    }

    .chapter-title {
        font-size: 0.8rem;
        font-weight: 700;
        color: var(--primary);
        padding: 0.5rem;
        background: var(--primary-light);
        border-radius: 6px;
        margin-bottom: 0.5rem;
    }

    .section-list {
        list-style: none;
        padding: 0;
        margin: 0;
    }

    .section-list li {
        margin-bottom: 2px;
    }

    .section-link {
        display: flex;
        align-items: flex-start;
        gap: 0.5rem;
        padding: 0.5rem;
        color: var(--text-muted);
        text-decoration: none;
        border-radius: 6px;
        font-size: 0.85rem;
        line-height: 1.4;
        transition: all 0.2s ease;
    }

    .section-link:hover {
        color: var(--primary);
        background: var(--bg-light);
    }

    .section-num {
        flex-shrink: 0;
        width: 20px;
        height: 20px;
        background: var(--border-light);
        color: var(--text-muted);
        font-size: 0.7rem;
        font-weight: 600;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .section-name {
        flex: 1;
    }

    /* Main Content */
    .main-content {
        min-width: 0;
    }

    /* Intro Section */
    .intro-section {
        margin-bottom: 2rem;
    }

    .intro-card {
        background: var(--bg-light);
        padding: 1.5rem;
        border-radius: 16px;
        margin-bottom: 1.5rem;
        border-left: 4px solid var(--primary);
    }

    .intro-card h2 {
        font-size: 1.25rem;
        margin: 0 0 1rem;
        color: var(--text-dark);
    }

    .intro-card p {
        color: var(--text-muted);
        line-height: 1.7;
        margin: 0;
    }

    .stats-grid {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 1rem;
    }

    @media (max-width: 640px) {
        .stats-grid {
            grid-template-columns: repeat(2, 1fr);
        }
    }

    .stat-item {
        background: white;
        padding: 1.25rem;
        border-radius: 12px;
        text-align: center;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
    }

    .stat-value {
        display: block;
        font-size: 1.75rem;
        font-weight: 700;
        color: var(--primary);
    }

    .stat-label {
        display: block;
        font-size: 0.85rem;
        color: var(--text-muted);
        margin-top: 0.25rem;
    }

    /* Chapters Overview */
    .chapters-overview {
        margin-bottom: 2rem;
    }

    .chapters-overview > h2 {
        font-size: 1.5rem;
        font-weight: 700;
        color: var(--text-dark);
        margin: 0 0 1.5rem;
    }

    .chapter-card {
        background: white;
        border-radius: 16px;
        margin-bottom: 1rem;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
        overflow: hidden;
    }

    .chapter-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 1rem 1.5rem;
        background: var(--primary);
        color: white;
    }

    .chapter-header h3 {
        font-size: 1rem;
        margin: 0;
    }

    .chapter-duration {
        font-size: 0.8rem;
        opacity: 0.9;
    }

    .sections-list {
        padding: 0.25rem 0;
    }

    .section-item {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        padding: 0.75rem 1.5rem;
        border-bottom: 1px solid var(--border-light);
        transition: background 0.2s ease;
        text-decoration: none;
        color: inherit;
    }

    .section-item:last-child {
        border-bottom: none;
    }

    .section-item:hover {
        background: var(--bg-light);
    }

    .section-item .section-num {
        width: 28px;
        height: 28px;
        background: var(--primary-light);
        color: var(--primary);
        font-weight: 600;
        font-size: 0.8rem;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
    }

    .section-item .section-title {
        flex: 1;
        color: var(--text-dark);
        font-size: 0.9rem;
    }

    .section-arrow {
        color: var(--text-muted);
        opacity: 0;
        transition: opacity 0.2s ease;
    }

    .section-item:hover .section-arrow {
        opacity: 1;
        color: var(--primary);
    }

    /* CTA */
    .cta-section {
        background: var(--bg-light);
        border-radius: 16px;
        padding: 2.5rem 2rem;
        text-align: center;
        border: 1px solid var(--border-light);
    }

    .cta-section h2 {
        font-size: 1.75rem;
        color: var(--text-dark);
        margin: 0 0 0.5rem;
    }

    .cta-section p {
        font-size: 1rem;
        color: var(--text-muted);
        margin: 0 0 1.5rem;
    }

    .cta-buttons {
        display: flex;
        justify-content: center;
        gap: 1rem;
        flex-wrap: wrap;
    }

    .btn {
        display: inline-flex;
        padding: 0.875rem 2rem;
        font-weight: 600;
        border-radius: 10px;
        text-decoration: none;
        transition: all 0.2s ease;
    }

    .btn-lg {
        padding: 1rem 2rem;
        font-size: 1rem;
    }

    .btn-primary {
        background: white;
        color: var(--primary);
    }

    .btn-primary:hover {
        transform: translateY(-2px);
    }

    .btn-accent {
        background: var(--accent);
        color: white;
    }

    .btn-accent:hover {
        transform: translateY(-2px);
        color: white;
    }
</style>
